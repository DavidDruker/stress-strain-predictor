# stress-strain-predictor
Predicts engineering stress–strain behavior of steels from chemical composition. Instead of fitting thousands of curve points, it learns the curve's landmarks — yield strength, UTS, elongation — with physically-consistent outputs (UTS > YS by construction) and grade-grouped evaluation that avoids the leakage random K-fold hides.
